<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.hyunprise.backend.domain.member.mappers.MemberMapper">
    <select id="selectOneMemberByMemberUUID" parameterType="String" resultType="Member">
        SELECT
            member_uuid,
            authentication_id,
            member_email,
            member_name,
            h_point,
            gender,
            birthdate,
            address,
            contact,
            account_type,
            brand_id
        FROM member
        WHERE member_uuid = #{memberUUID}
    </select>

    <select id="selectOneMemberByMemberEmail" parameterType="String" resultType="Member">
        SELECT
            member_uuid,
            authentication_id,
            member_email,
            member_name,
            h_point,
            gender,
            birthdate,
            address,
            contact,
            account_type,
            brand_id
        FROM member
        WHERE member_email = #{memberEmail}
    </select>

    <insert id="insertOneMember" parameterType="Member">
        INSERT INTO member (member_email, member_name, gender, h_point, account_type)
        VALUES (#{memberEmail}, #{memberName}, #{gender}, default, default)

        <selectKey keyProperty="memberUUID" resultType="String" order="AFTER">
            SELECT
                member_uuid
            FROM member
            WHERE member_email = #{memberEmail}
        </selectKey>
    </insert>
</mapper>